<div class='comments'>
    {{#each this}}
    <section class='card-comment'>
        <div class='name-date'>
            {{user.user.name}} on {{format_date created_at}}
        </div>
        <div class='text'>
            {{comment_text}}
        </div>

    </section>
</div>